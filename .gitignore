# Data (large files — keep locally, not in repo)
data/

# Outputs — track CSVs and numpy arrays, skip large binaries
outputs/*.pkl
outputs/cleaned_*.csv

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.env

# macOS
.DS_Store
